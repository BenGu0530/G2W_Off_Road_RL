[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "go2w_nav"
version = "0.1.0"
dependencies = [
    "robot_lab",
]

[tool.setuptools.packages.find]
where = [".."]
include = ["go2w_nav*"]